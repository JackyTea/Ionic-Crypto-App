import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'JTea_DPS926_Assignment4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
